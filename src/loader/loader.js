import { ping } from 'ldrs';

ping.register();
const loader = (
    <l-ping
      size="420"
      speed="2" 
      color="#d89b64"
      style={{margin:'auto', display: 'block'}}
    ></l-ping>  
)

export default loader